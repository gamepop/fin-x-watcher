# =============================================================================
# Financial Sentinel - Environment Configuration
# Hackathon Track: Grok × X API
# =============================================================================

# -----------------------------------------------------------------------------
# xAI / Grok API Configuration
# Get your API key from: https://console.x.ai
# -----------------------------------------------------------------------------
XAI_API_KEY=your_xai_api_key_here

# -----------------------------------------------------------------------------
# X API (Twitter) Configuration
# Get credentials from: https://developer.x.com/en/portal/dashboard
# Required for authenticated access to real X API endpoints
# -----------------------------------------------------------------------------
X_BEARER_TOKEN=your_x_bearer_token_here
X_API_KEY=your_x_api_key_here
X_API_SECRET=your_x_api_secret_here
X_ACCESS_TOKEN=your_x_access_token_here
X_ACCESS_TOKEN_SECRET=your_x_access_token_secret_here

# -----------------------------------------------------------------------------
# Slack Configuration
# Create a Slack App: https://api.slack.com/apps
#
# IMPORTANT: Use the Bot User OAuth Token, NOT the App-Level Token!
#   - Bot Token starts with: xoxb-  (CORRECT)
#   - App Token starts with: xapp-  (WRONG - won't work for posting messages)
#   - User Token starts with: xoxp- (WRONG - use bot token instead)
#
# Setup steps:
#   1. Go to https://api.slack.com/apps and create/select your app
#   2. Go to "OAuth & Permissions" in the sidebar
#   3. Under "Bot Token Scopes", add these scopes:
#      - chat:write (send messages as the bot)
#      - chat:write.public (post to public channels without joining)
#   4. Click "Install to Workspace" (or reinstall if adding scopes)
#   5. Copy the "Bot User OAuth Token" (starts with xoxb-)
#   6. For SLACK_CHANNEL_ID: right-click channel > View channel details > copy ID
# -----------------------------------------------------------------------------
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
SLACK_CHANNEL_ID=C0123456789

# -----------------------------------------------------------------------------
# Agent Configuration
# -----------------------------------------------------------------------------
MONITORING_INTERVAL_SECONDS=600
LOG_LEVEL=INFO

# Optional: Comma-separated list of banks to monitor
# TARGET_BANKS=Chase,Bank of America,Wells Fargo
